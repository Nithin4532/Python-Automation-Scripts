## File Renaming Automation

This script renames all files inside a folder automatically.

### How it works:
- Reads all files from a folder
- Renames them sequentially (file_1, file_2, etc.)

### Use Case:
Useful when handling large numbers of files.
